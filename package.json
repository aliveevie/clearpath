{
  "name": "clearpath",
  "version": "1.0.0",
  "description": "ClearPath — Compliance-Native Cross-Border Treasury on Solana",
  "scripts": {
    "test": "npx ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts",
    "build": "anchor build",
    "deploy": "anchor deploy",
    "deploy:devnet": "node scripts/deploy-devnet.js"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.32.0",
    "@solana/spl-token": "^0.4.12",
    "@solana/web3.js": "^1.98.0"
  },
  "devDependencies": {
    "@types/bn.js": "^5.2.0",
    "@types/chai": "^4.3.0",
    "@types/mocha": "^10.0.0",
    "@types/node": "^22.19.15",
    "chai": "^4.4.0",
    "mocha": "^10.7.0",
    "ts-mocha": "^10.0.0",
    "typescript": "^5.7.0"
  }
}
